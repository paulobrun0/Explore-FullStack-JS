// Tipagem explicita ou Anotação de tipo(Type Annotation)
let names: string[] = ["Paulo", "Vitor", "Rodrigo"];
let numbers: number[] = [1, 2, 3];

// Inferência de tipos
let products = ["Product x", "Product y", "Product z"];
