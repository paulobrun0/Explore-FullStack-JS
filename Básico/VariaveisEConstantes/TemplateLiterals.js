// Template literals (template strings, interpolação de strings)
let userName = "João";
let email = "joao@email.com";

// Passar masis de um parâmetro
console.log(userName, email, "teste");

// Concatenar texto.
let message = "Olá, " + userName + ". Você conectou com o email " + email;

console.log(message);

// Template literals
console.log(`Olá, ${userName}. Você conectou com o email ${email}`);
