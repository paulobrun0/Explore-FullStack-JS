// Arrow Function (Função de seta)
let showMessage = (message) => {
  console.log(message);
};

showMessage("Vai Corinthians");

const showUserName = (userName) => {
  console.log("Olá " + userName);
};

showUserName("João");
