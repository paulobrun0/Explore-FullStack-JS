let fullName = "Paulo Bruno";
console.log(fullName);

// Cria um Array com os nomes separados por espaço
console.log(fullName.split(" "));

// Cria um Array com as letras
console.log(Array.from(fullName));
