let message = "Estouestudando os fundamentos de JavaScript";

// Substituindo parte de um texto.
// console.log(
//   message.replace("os fundamentos de JavaScript", "Métodos de String")
// );

// Extraindo uma parte da string(start, end)
console.log(message.slice(0, 5));

// Extraindo uma parte da string de trás para frente
console.log(message.slice(-11));

let textWithSpace = "   Texto de exemplos   ";
console.log(textWithSpace.length);

// Excluindo espaço em branco no início e fim da string

console.log(textWithSpace.trim());
