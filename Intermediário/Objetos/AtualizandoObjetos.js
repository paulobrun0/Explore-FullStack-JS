const product = {
  name: "Teclado",
  quantity: 100,
};

// Acessando propriedade do objeto.
console.log(product.name);

// Atualizando valor de uma propriedade do objeto.

product.quantity = 200;
console.log(product.quantity);

product.price = 70;
console.log(product);
