// Função anônima (função que não possui um nome)

const sayHello = function () {
  return "Olá.!";
};

console.log(sayHello());

const showMessage = function (name) {
  return "Olá " + name;
};

console.log(showMessage("João"));
