let balance = 1000;
let payment = 100;

// > Maior que
console.log(balance > payment);

// < Menor que
console.log(balance < payment);

// >= Maior ou igual a
balance = 120;
console.log(balance >= payment);

// <= Menor ou igual a
balance = 1000;
console.log(balance <= payment);
